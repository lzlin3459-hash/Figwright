# Figwright runtime dependencies — open source only.
# No originpro / OriginExt / PySide6 / win32com: Figwright never launches Origin.
# Versions below are the exact set validated in a clean Origin-free environment.
# Requires 64-bit CPython 3.11 or 3.12.
numpy==2.5.3
pandas==3.0.6
matplotlib==3.11.2
openpyxl==3.1.5
xlrd==2.0.2
pillow==12.3.0
PyYAML==6.0.3
jsonschema==4.26.0
