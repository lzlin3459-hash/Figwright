# 桑基图视觉合同

- 使用 Origin 官方 Sankey 绘图入口，Source/Target/Value 三列保持真实可编辑。
- 示例覆盖多阶段、分支与汇合；流带采用来源到目标的渐变关系和克制透明度。
- 节点按流程深度使用低饱和色系，同一深度只改变明度；颜色列表通过 Origin 工作簿内部
  `__SankeyColor` helper column 应用并逐项反读，不写回原始数据。
- 页面随边数和节点密度扩展，标签使用可读物理字号；高密度网络会提示人工复核。
- 不手工拼接位图，标签遮挡可在可编辑 OPJU 内调整。
