# Optional, frozen dependency set for the OPTIONAL Origin backup backend.
# Installed ONLY into the product-managed ".venv-origin" by `setup.cmd --with-origin`.
# It is intentionally separate from the main .venv (which uses numpy 2.x / pandas 3.x),
# because originpro/OriginExt are validated against the pinned older stack.
# Never install these into the main .venv.
numpy==1.26.4
pandas==2.3.3
PyYAML==6.0.3
jsonschema==4.26.0
matplotlib==3.10.9
OriginExt==1.2.5
originpro==1.1.15
openpyxl==3.1.5
xlrd==2.0.2
pillow==12.3.0
