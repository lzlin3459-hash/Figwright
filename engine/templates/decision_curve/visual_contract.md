# 视觉合同

白底 Arial；阈值轴根据输入的实际有限范围自适应并严格限制在 0–1，Y 轴依据模型净获益自适应并明确包含零，避免高阈值处极端
`Treat all` 值压扁主要证据；被裁剪的参考线仍完整保留在 Origin 工作簿并生成明确警告。
模型曲线使用低饱和医学蓝青色族，`Treat all` 使用灰色虚线、`Treat none` 使用浅灰点线。所有曲线、
字号、轴框与图例均在 Origin 中反读。
