# 气泡图视觉合同

- 使用圆形、克制同色系填充、清楚边缘和适度透明度。
- 保持 X/Y 的真实数据意义，并使用气泡专用轴边距避免最大点被裁切。
- Origin 2024b 原生 Bubble Scale 的嵌套字号不能可靠反读，因此使用可编辑的 16 pt
  “Bubble area = Size (min-max)”映射说明，并与 UI 预览保持一致。
- 自适应画幅、Arial 物理字号、轴线和边框均需 Origin 反读。
