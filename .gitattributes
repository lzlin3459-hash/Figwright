# Normalize line endings for cross-platform clones.
* text=auto eol=lf

# Windows launchers must keep CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets (none currently, kept for future).
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.zip binary
